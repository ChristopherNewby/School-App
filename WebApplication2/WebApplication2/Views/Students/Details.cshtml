@model WebApplication2.Models.Class2


@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Student</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.newStudent.StudentName)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.newStudent.StudentName)
        </dd> 

        <dt>
            Courses
        </dt>

        <dd>

        </dd>

        @foreach (var item in Model.newCourse)
        {

            <dt>
                @Html.DisplayNameFor(modelItem => item.CourseName)
            </dt>

            <dd>
                @Html.DisplayFor(modelItem => item.CourseName)
            </dd>
        }
                              
    </dl>
</div>

@*<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.newStudent.StudentId }) |
    @Html.ActionLink("Back to List", "Index")
</p>*@
